---
platform: linux

# This task configuraiton file is used for testing only. Stand-alone execution of the task can 
# be run with the following fly command.
#
# rm -fr .test-data && mkdir -p .test-data; touch .test-data/name; touch .test-data/metadata
# fly -t stcs-jed e -c prepare-opsman-backup.yml -i pipeline-src=../ -i backup-metadata=.test-data -o backup-timestamp=.test-data -o job-session=.test-data

image_resource:
  type: docker-image
  source: 
    repository: appbricks/automation-tools

inputs:
- name: pipeline-src
- name: backup-metadata

run:
  path: pipeline-src/scripts/prepare-opsman-backup.sh

outputs:
- name: backup-timestamp
- name: job-session

params:
  # Copy params from backup-pcf.yml pipeline 
  # and paste here before testing
