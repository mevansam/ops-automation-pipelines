---
platform: linux

# This task configuraiton file is used for testing only. Stand-alone execution of the task can 
# be run with the following fly command.
#
# rm -fr .backup-metadata && mkdir -p .backup-metadata; touch .backup-metadata/name; touch .backup-metadata/metadata
# fly -t stcs-jed e -c prepare-backup.yml -i pipeline-src=../ -i backup-metadata=.backup-metadata -o backup-timestamp=.backup-metadata

image_resource:
  type: docker-image
  source: 
    repository: appbricks/automation-tools

inputs:
- name: pipeline-src
- name: backup-metadata

run:
  path: pipeline-src/scripts/prepare-backup.sh

outputs:
- name: backup-timestamp

params:
  # Copy params from backup-pcf.yml pipeline 
  # and paste here before testing
